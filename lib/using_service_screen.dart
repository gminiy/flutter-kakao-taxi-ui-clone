import 'package:flutter/material.dart';

class UsingServiceScreen extends StatelessWidget {
  const UsingServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('이용 서비스'),
    );
  }
}
